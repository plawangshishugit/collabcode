function handleEditAnalytics(roomId, userId) {
  console.log("📊 EDIT", roomId, userId);

  // Later can:
  // - increment counters
  // - store in DB
  // - aggregate per user / per room
}

module.exports = { handleEditAnalytics };
